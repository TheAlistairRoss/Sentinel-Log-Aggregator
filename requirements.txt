# Microsoft Sentinel Log Aggregator - Python Dependencies
# Requires Python 3.11 only

# Azure SDK packages - Core authentication and monitoring (latest versions)
azure-identity>=1.25.0
azure-monitor-query>=2.0.0
azure-monitor-ingestion>=1.1.0
azure-core>=1.30.0

# Configuration management
python-dotenv>=1.0.0
PyYAML>=6.0.0

# Async HTTP client support
aiohttp>=3.13.0

# Data validation and parsing
pydantic>=2.0.0
