name: "tests_query_without_params"
destination_stream: "Custom-Reports_LogAggregatorHealth_CL"
description: "Used for log aggregator unit testing. This query should succeed with the column 'WithoutParams' and the value 'true'"
stream_name: "stream_incident_summary"

query: |
    print 
      WithoutParams = true